# generative-article
Generate Article using Gemini AI and Pexels

run app using
  ```sh
  node app.js
  ```
or using nodemon
```sh
  npm run start
  ```

access endpoint using `POST` method at 
```sh
  http://localhost:3000/
  ```
